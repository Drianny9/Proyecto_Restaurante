# Proyecto_Restaurante
Primer proyecto DAW2 de restaurante

Hacer una clase en controlador para cada una de las "paginas" a mostrar.
--Ahora se ve porque hay un include.--

Usar API de conversor de moneda

------LOCAL STORAGE y SESION STORAGE-------
se puede usar para guardar filtros por ejemplo

En el localstorage se quedan los datos eternamente

En el sesionstorage solo funcionan en esa pestaña, cuando se cierra desaparecen los datos
Los datos se guardan en la pestaña aplicación de "Inspeccionar"

usamos la función localstorage.setItem

Ejemplo:  localstorage.setItem("usuario" , "Drianny"); //Esto lo haremos en JavaScript
localstorage.getItem()

removeItem() //para eliminar
